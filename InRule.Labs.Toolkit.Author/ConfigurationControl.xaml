<UserControl x:Class="InRule.Addins.ScenarioManager.ConfigurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InRule.Addins.ScenarioManager"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0">Height</Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Height}" Margin="0,0,0,5"></TextBox>
        
        <Label Grid.Row="1" Grid.Column="0">Width</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Width}" Margin="0,0,0,5"></TextBox>
    </Grid>
</UserControl>
